name: Build Firmware

on:
  workflow_dispatch:
  push: 
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Compile firmware
      run: |
        chmod +x compile-with-docker.sh
        ./compile-with-docker.sh

    - name: Upload firmware artifact
      uses: actions/upload-artifact@v4
      with:
        name: gogufw-1.3.0-firmware
        path: |
          build/Fusion/gogufw.bin
          build/Fusion/gogufw.hex
          build/Fusion/gogufw.elf
